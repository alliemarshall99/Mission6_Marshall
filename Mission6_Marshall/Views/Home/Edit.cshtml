@model Film

@{
    ViewData["Title"] = "Edit Movie";
}

<h1>Edit Movie</h1>

<form asp-action="Edit" asp-route-id="@Model.MovieId" method="post">
    @Html.AntiForgeryToken()

    <!-- Display and edit movie properties -->

    <button type="submit">Save Changes</button>
</form>
